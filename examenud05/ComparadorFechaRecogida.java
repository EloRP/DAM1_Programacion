package examenud05;

import java.util.Comparator;

public class ComparadorFechaRecogida implements Comparator {
    
    @Override
    public int compare(Object o1, Object o2) {
        Objeto f1 = (Objeto) o1;
        Objeto f2 = (Objeto) o2;

        if (f1.recogido = false) {
        } else if (f2.recogido = false) {
        return f2.compareTo(f1.recogido);
        }

    }
}
